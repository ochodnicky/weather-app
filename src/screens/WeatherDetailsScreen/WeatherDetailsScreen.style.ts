import {StyleSheet} from 'react-native';

import {MD2Colors} from 'react-native-paper';

export const WeatherDetailsScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: MD2Colors.white,
  },
});
